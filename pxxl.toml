[build]
language = "python"
framework = "flask"
packageManager = "pip"
runtime = "3.12"
port = 3000
installCommand = "pip install -r requirements.txt"
buildCommand = ""
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT"

[packages]
build=["coreutils"]
